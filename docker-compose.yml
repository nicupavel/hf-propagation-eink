version: '3.8'

services:
  solar-server:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NODE_VERSION: 22 
    ports:
      - "5001:5000"
    environment:
      NODE_ENV: production
    restart: unless-stopped